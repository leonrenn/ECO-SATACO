"""File holds funtion that adds
a column to the dataframe which will be of categorical
data (1 for up, 0 for down in comparison to the day before.)
"""
import pandas as pd


def add_performance_column(tickers: str,
                           df=pd.Dataframe) -> pd.DataFrame:
    pass
